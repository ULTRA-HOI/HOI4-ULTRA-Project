logistics_trucks_deficit_1_strategy = {
    allowed = { always = yes }
    enable = {
        check_variable = { logi_truck_deficit_ratio < 0.2 }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
		value = 500
		id = motorized
    }
}

logistics_trucks_deficit_2_strategy = {
    allowed = { always = yes }
    enable = {
        AND = {
            NOT = { check_variable = { logi_truck_deficit_ratio < 0.2 } }
            check_variable = { logi_truck_deficit_ratio < 0.4 }
        }

    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
		value = 400
		id = motorized
    }
}

logistics_trucks_deficit_3_strategy = {
    allowed = { always = yes }
    enable = {
        AND = {
            NOT = { check_variable = { logi_truck_deficit_ratio < 0.4 } }
            check_variable = { logi_truck_deficit_ratio < 0.6 }
        }

    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
		value = 300
		id = motorized
    }
}

logistics_trucks_deficit_4_strategy = {
    allowed = { always = yes }
    enable = {
        AND = {
            NOT = { check_variable = { logi_truck_deficit_ratio < 0.6 } }
            check_variable = { logi_truck_deficit_ratio < 0.8 }
        }

    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
		value = 200
		id = motorized
    }
}

logistics_trucks_deficit_5_strategy = {
    allowed = { always = yes }
    enable = {
        AND = {
            NOT = { check_variable = { logi_truck_deficit_ratio < 0.8 } }
            check_variable = { logi_truck_deficit_ratio < 0.9 }
        }

    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
		value = 100
		id = motorized
    }
}

logistics_trucks_deficit_6_strategy = {
    allowed = { always = yes }
    enable = {
        AND = {
            NOT = { check_variable = { logi_truck_deficit_ratio < 0.9 } }
            check_variable = { logi_truck_deficit_ratio < 0.99 }
        }

    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = equipment_production_factor
		value = 50
		id = motorized
    }
}